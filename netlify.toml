# Configuración de despliegue para Netlify
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Fallback SPA: cualquier ruta sirve el index (buenas prácticas)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
